import java.util.ArrayList;

/**
 * Write a class called Main with a method with the signature public ArrayList<Movie> getMoviesWithLongTitles(final Movie[] movies).
 * This method's parameter is an array of Movie references (from the previous question).
 * If a Movie from that array has a title with more than 10 characters in it, the Movie is added to a local ArrayList which gets returned.
 * Use a foreach loop.
 * Do not use magic numbers.
 * Show all imports.
 * Use best practices we learned in class; do all work yourself.
 * */

public class Main
{
    public static void main(String[] args)
    {
        System.out.println("Hello world!");
    }

}